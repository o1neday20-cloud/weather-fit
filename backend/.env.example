# .env 파일을 복사해서 실제 값으로 채워주세요
# cp backend/.env.example backend/.env

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=weather_fit

# 백엔드 패키지 설치 (multer 추가됨)
# npm install express mysql2 cors dotenv multer
